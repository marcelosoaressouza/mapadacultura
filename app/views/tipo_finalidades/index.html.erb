<ul class="nav nav-tabs">
  <li class="active">
    <%= link_to "Todos", tipo_finalidades_path %>
  </li>
  <li>
    <%= link_to "Cadastrar", new_tipo_finalidade_path %>
  </li>
</ul>

<div class="tab-content">
  <div class="active tab-pane">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>#</th>
          <th>Nome</th>
          <th><%= t '.actions', :default => t("helpers.actions") %></th>
        </tr>
      </thead>

      <tbody>
        <% @tipo_finalidades.each do |tipo_finalidade| %>
        <tr>
          <td><%= link_to tipo_finalidade.id, tipo_finalidade_path(tipo_finalidade) %></td>
          <td><%= tipo_finalidade.nome %></td>
          <td> <%= link_to t('.edit', :default => t("helpers.links.edit")), edit_tipo_finalidade_path(tipo_finalidade), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")), tipo_finalidade_path(tipo_finalidade), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem Certeza?')) }, :class => 'btn btn-mini btn-danger' %> </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
