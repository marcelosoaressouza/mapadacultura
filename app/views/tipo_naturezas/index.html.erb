<ul class="nav nav-tabs">
  <li class="active">
    <%= link_to "Todos", tipo_naturezas_path %>
  </li>
  <li>
    <%= link_to "Cadastrar", new_tipo_natureza_path %>
  </li>
</ul>

<div class="tab-content">
  <div class="active tab-pane">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>#</th>
          <th>Nome</th>
          <th><%= t '.actions', :default => t("helpers.actions") %></th>
        </tr>
      </thead>

      <tbody>
        <% @tipo_naturezas.each do |tipo_natureza| %>
        <tr>
          <td><%= link_to tipo_natureza.id, tipo_natureza_path(tipo_natureza) %></td>
          <td><%= tipo_natureza.nome %></td>
          <td> <%= link_to t('.edit', :default => t("helpers.links.edit")), edit_tipo_natureza_path(tipo_natureza), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")), tipo_natureza_path(tipo_natureza), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem Certeza?')) }, :class => 'btn btn-mini btn-danger' %> </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
