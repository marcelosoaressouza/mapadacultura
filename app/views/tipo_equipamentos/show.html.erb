<ul class="nav nav-tabs">
  <li>
    <%= link_to "Todos", tipo_equipamentos_path %>
  </li>
  <li>
    <%= link_to "Cadastrar", new_tipo_equipamento_path %>
  </li>
  <li class="active">
    <%= link_to @tipo_equipamento.nome %>
  </li>
</ul>

<div class="tab-content">
  <div class="active tab-pane">
    <dl class="dl-horizontal">
      <dt>
        <strong>Nome:</strong>
      </dt>
      <dd>
        <%= @tipo_equipamento.nome %>&nbsp;
      </dd>
      <dt>
        <strong>Descrição:</strong>
      </dt>
      <dd>
        <%= @tipo_equipamento.descricao %>&nbsp;
      </dd>
      <dt>
        <strong>Imagem:</strong>
      </dt>
      <dd>
        <%= link_to (image_tag @tipo_equipamento.imagem.url(:thumb)), @tipo_equipamento.imagem.url %>&nbsp;
      </dd>
    </dl>

    <% if user_signed_in? %>
    <% if current_user.admin? %>
    <div class="form-actions">
      <%= link_to t('.back', :default => t("helpers.links.back")), tipo_equipamentos_path, :class => 'btn'  %>
      <%= link_to t('.edit', :default => t("helpers.links.edit")), edit_tipo_equipamento_path(@tipo_equipamento), :class => 'btn' %>
      <%= link_to t('.destroy', :default => t("helpers.links.destroy")), tipo_equipamento_path(@tipo_equipamento), :method => 'delete',
      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem Certeza?')) },
      :class => 'btn btn-danger' %>
    </div>
    <% end %>
    <% end %>
  </div>
</div>
